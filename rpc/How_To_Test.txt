# How to build and test the RPC server

In the root of the project, run :

sudo docker build -t geneweb-python -f docker/Dockerfile .

docker run -p 2316:2316 geneweb-python

==================================================

# Postman

POST : http://localhost:2316/rpc

body :
{
  "method": "ping",
  "params": {}
}

result :
{
    "result": "pong"
}